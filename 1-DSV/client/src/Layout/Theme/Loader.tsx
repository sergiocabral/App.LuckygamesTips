namespace Layout.Theme {

    /**
     * Tema de cores padrão.
     */
    export class Loader {

    //     /**
    //      * Retorna um tema de cores pelo nome.
    //      * @param {string} name Nome do tema.
    //      * @returns {Promise<Colors>} Tema de cores.
    //      */
    //     public static get(name: string): Promise<Colors> {
    //         return new Promise(resolve => {
    // //             const request = new XMLHttpRequest();
    // //             request.open("GET", yourUrl, false);
    // // Httpreq.send(null);
    // // return Httpreq.responseText; 
    //         });
    //     }
    }
}