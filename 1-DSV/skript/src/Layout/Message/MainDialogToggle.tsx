namespace Skript.Layout.Message {

    /**
     * Exibe ou esconde a janela principal.
         * @param {any} bind Opcional. Usada para fazer bind ao chamar o handler.
     */
    export class MainDialogToggle extends Core.Bus.Message { }
}